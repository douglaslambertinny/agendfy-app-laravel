# Agendfy with laravel

## Para executar o projeto

```$ docker compose up -d```

```$ docker compose exec app composer install```

```$ docker compose exec app artisan migrate```

```$ docker compose exec app npm install```